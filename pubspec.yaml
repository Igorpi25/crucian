name: crucian
description: Flutter DDD expirements.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.18.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.1
  equatable: ^2.0.5
  web_socket_channel: ^2.2.0
  json_annotation: ^4.7.0
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  shouldly: ^0.5.0+1
  build_runner: ^2.3.0
  json_serializable: ^6.5.1
  given_when_then_unit_test: ^0.1.0

flutter:
  uses-material-design: true
